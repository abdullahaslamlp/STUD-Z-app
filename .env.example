# Supabase — Project Settings → API (URL + anon/public key)
# Local dev: copy to `.env` (gitignored).
# Production builds also load `.env.production` (committed) so deploys embed these by default.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
